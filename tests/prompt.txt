Call `resources/list`, then read:
- `convertigo://capabilities`
- `convertigo://recipes/quickstart`
- `convertigo://resources/convertigo-start`
- `convertigo://resources/convertigo-engineering-workflow`
- `convertigo://resources/convertigo-contract-first-delivery`
- `convertigo://resources/convertigo-backend-sequences`
- `convertigo://resources/convertigo-integration-http`
- `convertigo://resources/convertigo-validation-and-evidence`

Confirm you refreshed those guides, then continue with this plan.

In the `codex_test` project, create or update a facade sequence `ip_info` backed by `https://api.ipify.org?format=json`. Lock the public response contract before the first write call.

Required public top-level fields:
- `status`: `ok`, `fallback`, or `error`
- `source`: `http` or `stub`
- `ip`: string
- `error`: string

Rules:
- Make the facade executable in stub mode first, then wire the real HTTP path behind the same contract.
- If the upstream call fails, the fallback path must keep the same top-level fields and types.
- The facade owns the public contract; the raw HTTP transaction does not.

Use MCP tools only, not local shell edits. Validate through MCP, save the project, and end with:
- the contract you used,
- one proof before or during stub mode,
- one proof after wiring the HTTP path,
- one short "MCP critique" describing what felt confusing, which tools or guides were missing, and any UX improvement that would have helped.
