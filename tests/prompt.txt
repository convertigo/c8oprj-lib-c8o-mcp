Lis seulement `convertigo_mcp_usage`, puis crée dans `codex_test` une séquence `ip_info` qui retourne la réponse de `https://api.ipify.org?format=json` (fallback si échec). Utilise uniquement les outils MCP (pas de commandes locales) et respecte l’input schema (`related` attendu). Teste via MCP et finis par une critique du MCP.
