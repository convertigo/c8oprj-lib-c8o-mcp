Call `resources/list`, then read:
- `convertigo://capabilities`
- `convertigo://recipes/quickstart`
- `convertigo://resources/convertigo-start`
- `convertigo://resources/convertigo-engineering-workflow`
- `convertigo://resources/convertigo-contract-first-delivery`
- `convertigo://resources/convertigo-backend-sequences`
- `convertigo://resources/convertigo-integration-http`
- `convertigo://resources/convertigo-validation-and-evidence`

Confirm you refreshed those guides, then continue with this probe.

In the `codex_test` project, create or update a facade sequence named `probe_http_contract` backed by `https://api.ipify.org?format=json`.

Required public contract:
- top-level fields:
  - `status`: `ok`, `fallback`, or `error`
  - `source`: `http` or `stub`
  - `ip`: string
  - `error`: string

Rules:
1. Make the facade executable in stub mode first and prove it runs.
2. Create or update the HTTP integration behind that facade.
3. Replace the stub internals without changing the public top-level fields.
4. If the upstream call fails, keep the same top-level fields and types through the fallback path.
5. Save the project.

Final output format:
- `RESULT: PASS` only if the facade runs in stub mode, then runs again with the HTTP path wired, and both proofs keep the same top-level fields.
- `RESULT: FAIL` otherwise, with the failing step and tool response snippets.
- End with one short `MCP critique` item.
