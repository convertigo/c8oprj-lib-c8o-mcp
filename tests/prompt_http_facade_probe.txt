This is an HTTP specialist probe.
- Preferred runner: inject MCP role prompt `convertigo-http` before this scenario starts.
- If no role prompt was injected, fetch `convertigo-http` yourself before continuing.
- Choose the target project in this order: `SqlBlob`, `BaserowIntegration`, `sample_HelloWorld`. Use the first one that exists and report the chosen project name.
- If none of these exists, choose the first loaded project whose comment is `Mashup Sequencer project`, whose name does not start with `lib_`, and whose name is not `ConvertigoMCP` or `couchDB`. Report the chosen fallback explicitly.

In the chosen project, create or update a facade sequence named `probe_http_contract` backed by `https://api.ipify.org?format=json`.
Use a concrete HTTP connector and transaction path:
- connector class: `connectors.HttpConnector`
- transaction class: `transactions.JsonHttpTransaction`
- connector host: `api.ipify.org`
- connector settings: `https=true`, `port=443`, `baseDir=""`
- transaction sub path: `/?format=json`
- keep `httpInfo=true` during setup

If you need a live structural reference for an HTTP connector and JSON transaction, inspect `BaserowIntegration.Baserow` before writing.

Required public contract:
- top-level fields:
  - `status`: `ok`, `fallback`, or `error`
  - `source`: `http` or `stub`
  - `ip`: string
  - `error`: string

Rules:
1. Make the facade executable in stub mode first and prove it runs.
2. Create or update the HTTP integration behind that facade.
3. Replace the stub internals without changing the public top-level fields.
4. Map the upstream `ip` field into the facade `ip` field. Do not leak raw connector field names at the public facade level.
5. If the upstream call fails, keep the same top-level fields and types through the fallback path.
6. Save the project.

Final output format:
- `RESULT: PASS` only if the facade runs in stub mode, then runs again with the HTTP path wired, and both proofs keep the same top-level fields.
- `RESULT: FAIL` otherwise, with the failing step and tool response snippets.
- End with one short `MCP critique` item.
