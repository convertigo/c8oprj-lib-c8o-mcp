#!/usr/bin/env bash
set -euo pipefail

# Single source of truth for the Codex CLI version used by test runs.
CODEX_NPM_VERSION="${CODEX_NPM_VERSION:-0.111.0}"

exec npx -y "@openai/codex@${CODEX_NPM_VERSION}" "$@"
