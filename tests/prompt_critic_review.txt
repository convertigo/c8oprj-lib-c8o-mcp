This is a critic review probe.
- Preferred runner: inject MCP role prompt `convertigo-critic` before this scenario starts.
- If no role prompt was injected, fetch `convertigo-critic` yourself before continuing.
- Review the most recent probe log under `../logs/` relative to the current workspace if at least one log exists.
- If no prior probe log exists, stop with `RESULT: SKIPPED - no prior probe log found`.

Review goals:
- identify concrete findings first
- call out missing runtime evidence if the implementation claims more than it proved
- report any guide misuse or contract drift you can support from the log and live MCP state
- end with one short MCP UX critique

Final output format:
- `RESULT: PASS` only if you reviewed one concrete probe log and produced findings or a justified `no findings` conclusion
- `RESULT: SKIPPED` for the allowed missing-log case
- `RESULT: FAIL` otherwise, with the failing step and tool response snippets
- End with one short `MCP critique` item.
