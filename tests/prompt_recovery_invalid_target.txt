This is a backend recovery probe.
- Preferred runner: inject MCP role prompt `convertigo-backend` before this scenario starts.
- If no role prompt was injected, fetch `convertigo-backend` yourself before continuing.
- Choose the target project in this order: `SqlBlob`, `BaserowIntegration`, `sample_HelloWorld`. Use the first one that exists and report the chosen project name.
- If none of these exists, choose the first loaded project whose comment is `Mashup Sequencer project`, whose name does not start with `lib_`, and whose name is not `ConvertigoMCP` or `couchDB`. Report the chosen fallback explicitly.

Assume the initial target parent QName is `<chosen project>.MissingParent` until MCP proves otherwise.
Rules:
1. Do not mutate `MissingParent`.
2. Detect and report the invalid target explicitly.
3. Re-discover the correct parent under the chosen project root.
4. Create or update a sequence named `probe_recovery_target` under the correct project root.
5. Add one optional string input variable named `name`.
6. The sequence must return these top-level fields:
   - `status`: `ok`
   - `message`: string
7. Validate the runtime with `requestable-execute`.
8. Save the project.

Required final output sections:
- `Changed Objects`
- `Recovery Evidence`
- `Runtime Evidence`
- `Open Handoff`

Final output format:
- `RESULT: PASS` only if you explicitly report the invalid target, identify the corrected target, create or update `probe_recovery_target`, validate it successfully, and save the project.
- `RESULT: FAIL` otherwise, with the failing step and tool response snippets.
- End with one short `MCP critique` item.
