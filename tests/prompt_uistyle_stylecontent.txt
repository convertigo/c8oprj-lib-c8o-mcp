Call `resources/list`, then read:
- `convertigo://capabilities`
- `convertigo://recipes/quickstart`
- `convertigo://resources/convertigo-start`
- `convertigo://resources/convertigo-frontend-ngx`
- `convertigo://resources/convertigo-validation-and-evidence`

Then call `project-list`, then locate an NGX app root QName (class `ngx.components.ApplicationComponent`) by using `databaseobject-search` on each candidate project if needed.

Run this regression with MCP tools only (no local shell edits):

1. Pick the NGX app root:
- Prefer `MyVibeProject.Application.NgxApp` if it exists.
- Otherwise auto-discover the first available NGX app QName.
- If no NGX app exists, stop and report `SKIPPED: no NGX app found`.

2. Create a temporary `ngx.components.UIStyle` under that NGX app root with one call:
- target: `<ngx app qname>`
- at: `inside`
- tree:
  - name: `CodexStyleRegressionProbe`
  - className: `ngx.components.UIStyle`

3. Test property alias mapping through tree-apply:
- Call `databaseobject-tree-apply` on `<probe qname>` with `at=self` and `tree.properties` set to `{"content": ".probe { color: red; }"}`.
- Read back with `databaseobject-tree-get` (`childrenDepth=0`, `properties=all`) and verify `styleContent` equals `.probe { color: red; }`.

4. Test direct property write:
- Call `databaseobject-tree-apply` with `{"styleContent": ".probe { color: blue; background: #fff; }"}`.
- Read back and verify exact equality.

5. Test XMLizable-shaped payload compatibility:
- Call `databaseobject-tree-apply` with:
  `{"styleContent":{"root":"com.twinsoft.convertigo.beans.common.FormatedContent","data":{"→":".probe { padding: 4px; }"}}}`
- Read back and verify exact equality.

6. Cleanup:
- Delete `CodexStyleRegressionProbe` with `databaseobject-delete`.
- Confirm it no longer exists using `databaseobject-search`.

7. Final output format:
- `RESULT: PASS` only if all 3 write/read checks passed and cleanup succeeded.
- Otherwise `RESULT: FAIL` with the failing step and tool response snippets.
- End with one short `MCP critique` item.
