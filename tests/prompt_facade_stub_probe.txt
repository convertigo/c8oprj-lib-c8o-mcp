This is a planner probe.
- Preferred runner: inject MCP role prompt `convertigo-planner` before this scenario starts.
- If no role prompt was injected, fetch `convertigo-planner` yourself before continuing.
- Benchmark policy: do not use local YAML-editing skills or the `convertigo-project-editor` workflow for this scenario. Use MCP tools only for Convertigo project mutations.
- Use `project-list(limit=50)` first, or continue pagination until every preferred project candidate has been checked. Do not inspect or mutate a fallback project while an earlier preferred candidate is still unchecked.
- Choose the target project in this order: `SqlBlob`, `BaserowIntegration`, `sample_HelloWorld`. Use the first one that exists and report the chosen project name.
- If none of these exists, choose the first loaded project whose comment is `Mashup Sequencer project`, whose name does not start with `lib_`, and whose name is not `ConvertigoMCP` or `couchDB`. Report the chosen fallback explicitly.

In the chosen project, create or update a facade sequence named `probe_contract_stub`. This probe is stub-only: do not create any connector transaction for it.

Fast-path rules:
- First, search for `probe_contract_stub` in the chosen project.
- If the sequence already exists, prefer one exact subtree read on that sequence and the minimum targeted update needed to satisfy the contract.
- Do not perform a broad project-wide `databaseobject-tree-get` when an exact search result already gives the target QName.
- Reuse the existing facade when it already matches the required contract instead of rebuilding it.

Required public contract:
- input variable: `query` (string, optional)
- top-level fields:
  - `status`: `ok` or `error`
  - `source`: always `stub`
  - `items`: array of objects with `id` and `label`
  - `total`: integer
  - `error`: string

Rules:
1. Lock the contract before the first write call.
2. Make the sequence executable immediately.
3. Keep the response shape stable for both nominal and error cases.
4. Validate it with `requestable-execute`.
5. Save the project.
6. Produce explicit handoff guidance for backend, SQL or HTTP, frontend NGX, and critic follow-up.

Final output format:
- `RESULT: PASS` only if the chosen project is the first existing preferred candidate (or the explicit fallback after proving all preferred candidates absent), and the sequence exists, executes, and returns exactly the required top-level fields with `source="stub"`.
- Otherwise `RESULT: FAIL` with the failing step and tool response snippets.
- End with one short `MCP critique` item.
