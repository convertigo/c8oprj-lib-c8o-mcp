This is a backend specialist probe.
- Preferred runner: inject MCP role prompt `convertigo-backend` before this scenario starts.
- If no role prompt was injected, fetch `convertigo-backend` yourself before continuing.
- Benchmark policy: do not use local YAML-editing skills or the `convertigo-project-editor` workflow for this scenario. Use MCP tools only for Convertigo project mutations.
- Before the first write call, state whether `convertigo://resources/convertigo-contract-first-delivery` is needed for this task and why.
- Choose the target project in this order: `SqlBlob`, `BaserowIntegration`, `sample_HelloWorld`. Use the first one that exists and report the chosen project name.
- If none of these exists, choose the first loaded project whose comment is `Mashup Sequencer project`, whose name does not start with `lib_`, and whose name is not `ConvertigoMCP` or `couchDB`. Report the chosen fallback explicitly.

In the chosen project, create a sequence `analyze_sentence` with one input variable `sentence` (string). The sequence must return a JSON document containing a `words` array. Each entry must include:
- `text`: the word lowercased with simple punctuation removed,
- `length`: number of characters,
- `vowels`: count of vowels (a,e,i,o,u,y),
- `consonants`: count of consonants.

Test early with `requestable-execute` on `<chosen project>.analyze_sentence` using `sentence=Hello`; keep the final proof with `sentence=Hello world!`. If `analyze_sentence` already exists, run the tests and confirm current behaviour.
End the run with a short "MCP critique" describing what felt confusing, which tools were missing, and any UX improvements that would have helped.
