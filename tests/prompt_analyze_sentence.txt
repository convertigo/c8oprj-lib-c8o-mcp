This is a backend specialist probe.
- Benchmark policy: the campaign runner already injected `convertigo-backend`.
- Benchmark policy: do not use local YAML-editing skills or the `convertigo-project-editor` workflow for this scenario. Use MCP tools only for Convertigo project mutations.
- Before the first write call, state whether `convertigo://resources/convertigo-contract-first-delivery` is needed for this task and why.
- Target project: `__TARGET_PROJECT__`
- This project was imported by the campaign runner from `__FIXTURE_SOURCE_PROJECT__`.
- Mutate only `__TARGET_PROJECT__`.
- If `__TARGET_PROJECT__` is missing, fail immediately.

In `__TARGET_PROJECT__`, create a sequence `analyze_sentence` with one input variable `sentence` (string). The sequence must return a JSON document containing a `words` array. Each entry must include:
- `text`: the word lowercased with simple punctuation removed,
- `length`: number of characters,
- `vowels`: count of vowels (a,e,i,o,u,y),
- `consonants`: count of consonants.

Required workflow:
1. Search for the exact target first. If `analyze_sentence` already exists and already matches the contract, reuse it.
2. If you reuse an existing sequence, prove it with:
   - `requestable-execute(sentence=Hello)`
   - `requestable-execute(sentence=Hello world!)`
   - one structural readback of the exact sequence subtree
3. If you mutate, validate early with `sentence=Hello`, keep the final proof with `sentence=Hello world!`, and save the project.
4. If you do not mutate, state explicitly that no save was needed because the existing implementation was reused unchanged.

Final output:
- `RESULT: PASS` only if `__TARGET_PROJECT__` is named explicitly, the sequence exists at the end, both runtime proofs succeed, and the `words` entries contain the required keys.
- `RESULT: FAIL` otherwise, with the failing step and tool response snippets.
- End with one short `MCP critique` item describing what felt confusing, which tools were missing, and any UX improvements that would have helped.
